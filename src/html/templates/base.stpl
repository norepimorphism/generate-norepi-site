<!doctype html>
<html lang="en">
    <head>
        <title><%= title %></title>
        <meta charset="utf-8">
        <meta name="author" content="norepimorphism">
        <meta name="theme-color" content="#6842f4">
        <meta name="color-scheme" content="dark">
        <script src="coi-serviceworker.min.js"></script>
        <% for stylesheet in stylesheets { %>
            <link rel="stylesheet" href="<%= stylesheet %>">
        <% } %>
    </head>
    <body>
        <header>
            <h1><%= title %></h1>
        </header>
        <main>
            <%- main %>
        </main>
        <hr>
        <footer>Contact me at <address style="display: inline"><pre style="display: inline">&lt;the title of this webpage&gt;</pre>@protonmail.com</address>.</footer>
    </body>
</html>
